function NotFound() {
    return (
        <h1
            className="d-flex align-items-center justify-content-center"
            style={{ color: 'red', fontWeight: '600', minHeight: '50vh' }}>
            Địa chỉ tìm kiếm không tồn tại!
        </h1>
    );
}

export default NotFound;